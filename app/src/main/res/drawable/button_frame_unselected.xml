<?xml version="1.0" encoding="utf-8"?>
<shape
  xmlns:android="http://schemas.android.com/apk/res/android"
  android:shape="rectangle">

  <solid
    android:color="?attr/colorSurface"/>

  <stroke
    android:color="?attr/colorOnSurfaceVariant"
    android:width="@dimen/outline_width_unselected"
    android:dashWidth="@dimen/dash_length"
    android:dashGap="@dimen/dash_gap"/>

  <size
    android:width="@dimen/control_size"
    android:height="@dimen/control_size"/>

</shape>